FROM node:18-alpine

WORKDIR /app

COPY package.json /app/package.json
RUN npm install

COPY . /app

EXPOSE 3000

# Start Vite dev server exposing all network hosts
CMD ["npm", "run", "dev", "--", "--host", "0.0.0.0", "--port", "3000"]
